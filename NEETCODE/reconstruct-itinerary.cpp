
/**
* @file reconstruct-itinerary.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/reconstruct-itinerary
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "NEETCODE" "reconstruct-itinerary"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    vector<string> findItinerary(vector<vector<string>>& tickets) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    