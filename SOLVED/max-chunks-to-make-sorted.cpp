
/**
* @file max-chunks-to-make-sorted.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/max-chunks-to-make-sorted
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "max-chunks-to-make-sorted"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    int maxChunksToSorted(vector<int>& arr) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    