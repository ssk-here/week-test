
/**
* @file sum-of-prefix-scores-of-strings.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/sum-of-prefix-scores-of-strings
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "sum-of-prefix-scores-of-strings"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    vector<int> sumPrefixScores(vector<string>& words) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    