
/**
* @file backspace-string-compare.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/backspace-string-compare
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat SEAN_PRASAD backspace-string-compare
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    bool backspaceCompare(string s, string t) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    