
/**
* @file longest-subarray-of-1s-after-deleting-one-element.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/longest-subarray-of-1s-after-deleting-one-element
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "longest-subarray-of-1s-after-deleting-one-element"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    int longestSubarray(vector<int>& nums) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    