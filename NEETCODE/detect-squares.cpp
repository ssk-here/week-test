
/**
* @file detect-squares.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/detect-squares
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "NEETCODE" "detect-squares"
*/
#include <allheaders.h>
using namespace std;
class DetectSquares {
public:
    DetectSquares() {
        
    }
    
    void add(vector<int> point) {
        
    }
    
    int count(vector<int> point) {
        
    }
};

/**
 * Your DetectSquares object will be instantiated and called as such:
 * DetectSquares* obj = new DetectSquares();
 * obj->add(point);
 * int param_2 = obj->count(point);
 */
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    