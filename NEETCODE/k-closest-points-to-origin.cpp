
/**
* @file k-closest-points-to-origin.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/k-closest-points-to-origin
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "NEETCODE" "k-closest-points-to-origin"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    vector<vector<int>> kClosest(vector<vector<int>>& points, int k) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    