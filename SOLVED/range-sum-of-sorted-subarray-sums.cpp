
/**
* @file range-sum-of-sorted-subarray-sums.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/range-sum-of-sorted-subarray-sums
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "range-sum-of-sorted-subarray-sums"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    int rangeSum(vector<int>& nums, int n, int left, int right) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    