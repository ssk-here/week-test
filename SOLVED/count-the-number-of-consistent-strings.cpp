
/**
* @file count-the-number-of-consistent-strings.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/count-the-number-of-consistent-strings
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "count-the-number-of-consistent-strings"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    int countConsistentStrings(string allowed, vector<string>& words) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    