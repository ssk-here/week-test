
/**
* @file minimum-interval-to-include-each-query.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/minimum-interval-to-include-each-query
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "NEETCODE" "minimum-interval-to-include-each-query"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    vector<int> minInterval(vector<vector<int>>& intervals, vector<int>& queries) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    