
/**
* @file kth-largest-element-in-an-array.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/kth-largest-element-in-an-array
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat SEAN_PRASAD kth-largest-element-in-an-array
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    int findKthLargest(vector<int>& nums, int k) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    