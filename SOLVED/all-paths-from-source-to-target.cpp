
/**
* @file all-paths-from-source-to-target.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/all-paths-from-source-to-target
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "all-paths-from-source-to-target"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    vector<vector<int>> allPathsSourceTarget(vector<vector<int>>& graph) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    