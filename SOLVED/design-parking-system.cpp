
/**
* @file design-parking-system.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/design-parking-system
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "design-parking-system"
*/
#include <allheaders.h>
using namespace std;
class ParkingSystem {
public:
    ParkingSystem(int big, int medium, int small) {
        
    }
    
    bool addCar(int carType) {
        
    }
};

/**
 * Your ParkingSystem object will be instantiated and called as such:
 * ParkingSystem* obj = new ParkingSystem(big, medium, small);
 * bool param_1 = obj->addCar(carType);
 */
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    