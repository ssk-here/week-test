
/**
* @file container-with-most-water.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/container-with-most-water
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "NEETCODE" "container-with-most-water"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    int maxArea(vector<int>& height) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    