
/**
* @file bitwise-and-of-numbers-range.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/bitwise-and-of-numbers-range
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "bitwise-and-of-numbers-range"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    int rangeBitwiseAnd(int left, int right) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    