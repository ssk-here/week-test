
/**
* @file reverse-bits.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/reverse-bits
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "reverse-bits"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    uint32_t reverseBits(uint32_t n) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    