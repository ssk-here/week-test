
/**
* @file minimum-cost-for-tickets.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/minimum-cost-for-tickets
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "minimum-cost-for-tickets"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    int mincostTickets(vector<int>& days, vector<int>& costs) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    