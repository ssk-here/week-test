
/**
* @file median-of-two-sorted-arrays.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/median-of-two-sorted-arrays
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat SEAN_PRASAD median-of-two-sorted-arrays
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    