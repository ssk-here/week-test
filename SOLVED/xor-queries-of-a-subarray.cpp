
/**
* @file xor-queries-of-a-subarray.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/xor-queries-of-a-subarray
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "xor-queries-of-a-subarray"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    vector<int> xorQueries(vector<int>& arr, vector<vector<int>>& queries) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    