
/**
* @file fraction-to-recurring-decimal.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/fraction-to-recurring-decimal
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "fraction-to-recurring-decimal"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    string fractionToDecimal(int numerator, int denominator) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    