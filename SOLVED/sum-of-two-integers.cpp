
/**
* @file sum-of-two-integers.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/sum-of-two-integers
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "sum-of-two-integers"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    int getSum(int a, int b) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    