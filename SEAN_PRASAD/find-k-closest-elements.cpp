
/**
* @file find-k-closest-elements.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/find-k-closest-elements
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat SEAN_PRASAD find-k-closest-elements
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    vector<int> findClosestElements(vector<int>& arr, int k, int x) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    