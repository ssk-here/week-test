
/**
* @file maximum-length-of-pair-chain.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/maximum-length-of-pair-chain
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "maximum-length-of-pair-chain"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    int findLongestChain(vector<vector<int>>& pairs) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    