
/**
* @file find-the-length-of-the-longest-common-prefix.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/find-the-length-of-the-longest-common-prefix
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "find-the-length-of-the-longest-common-prefix"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    int longestCommonPrefix(vector<int>& arr1, vector<int>& arr2) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    