
/**
* @file maximum-depth-of-binary-tree.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/maximum-depth-of-binary-tree
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat SEAN_PRASAD maximum-depth-of-binary-tree
*/
#include <allheaders.h>
using namespace std;
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    int maxDepth(TreeNode* root) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    