
/**
* @file regular-expression-matching.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/regular-expression-matching
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "NEETCODE" "regular-expression-matching"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    bool isMatch(string s, string p) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    