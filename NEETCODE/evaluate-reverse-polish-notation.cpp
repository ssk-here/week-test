
/**
* @file evaluate-reverse-polish-notation.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/evaluate-reverse-polish-notation
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "NEETCODE" "evaluate-reverse-polish-notation"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    int evalRPN(vector<string>& tokens) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    