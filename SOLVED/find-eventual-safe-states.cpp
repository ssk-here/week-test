
/**
* @file find-eventual-safe-states.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/find-eventual-safe-states
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "SOLVED" "find-eventual-safe-states"
*/
#include <allheaders.h>
using namespace std;
class Solution {
public:
    vector<int> eventualSafeNodes(vector<vector<int>>& graph) {
        
    }
};
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    