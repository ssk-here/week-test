
/**
* @file kth-largest-element-in-a-stream.cpp
* @author [Your Name]
* @date [Current Date]
* @see https://leetcode.com/problems/kth-largest-element-in-a-stream
* @brief   
* Time Complexity: 
* Space Complexity: 
* Build using .\build.bat "NEETCODE" "kth-largest-element-in-a-stream"
*/
#include <allheaders.h>
using namespace std;
class KthLargest {
public:
    KthLargest(int k, vector<int>& nums) {
        
    }
    
    int add(int val) {
        
    }
};

/**
 * Your KthLargest object will be instantiated and called as such:
 * KthLargest* obj = new KthLargest(k, nums);
 * int param_1 = obj->add(val);
 */
void Test(const std::string& inputfile, const std::string& outputfile) {
;    
}

int main(int argc, char* argv[]) {

    Common().parseArguments(argc,argv);

    Test(argv[1], argv[2]);

    return 0;
}
    